# LibrarySite 메인 페이지 테스트 계획서

본 테스트 계획서는 프로젝트의 메인 페이지에 대한 주요 기능 및 UI/UX 상호작용, 그리고 웹 접근성을 검증하기 위해 작성되었습니다. 모든 테스트 시나리오는 BDD(Behavior-Driven Development) 기반의 **Given-When-Then** 형식을 엄격히 준수합니다.

---

## 1. 웹 접근성 (Web Accessibility) 자동화 검증

**Scenario 1-1: 메인 페이지 기본 접근성 무결성 확인**
* **Given**: 사용자가 다양한 기기(데스크톱, 태블릿, 모바일) 환경에서 로컬 테스트 서버(`http://localhost:5173/`)의 메인 페이지에 접속한 상태에서
* **When**: Playwright에 통합된 Axe-core 웹 접근성 자동화 분석 도구를 실행하면
* **Then**: 모든 UI 요소에서 심각도(Serious/Critical)를 포함한 어떠한 접근성 위반 사항(Violations)도 발견되지 않아야 한다.

**Scenario 1-2: 키보드 네비게이션 포커스 확인**
* **Given**: 마우스를 사용할 수 없는 사용자가 메인 페이지에 처음 진입한 상태에서
* **When**: `Tab` 키를 눌러 페이지 내부의 모든 상호작용 가능한 요소(링크, 버튼, 입력창, 캐러셀 트랙 등)를 순차적으로 탐색하면
* **Then**: 모든 상호작용 요소에 시각적으로 명확한 포커스 링(Focus Ring)이 표시되어야 하며, 포커스가 갇히는(Keyboard Trap) 구간이 없어야 한다.

---

## 2. 태블릿 환경 반응형 레이아웃 및 팝업존 (Tablet PopupZone)

**Scenario 2-1: 팝업존 캐러셀 썸네일 동기화**
* **Given**: 화면 너비가 태블릿 해상도(481px 이상)로 설정된 브라우저에서 메인 페이지의 팝업존(PopupZone) 영역이 렌더링되었을 때
* **When**: 사용자가 팝업존의 '다음(Next)' 컨트롤 버튼을 클릭하거나 특정 썸네일 아이템을 터치하면
* **Then**: 메인 배너 이미지가 즉시 변경되어야 하며, 동시에 수직 썸네일 캐러셀이 선택된 항목의 위치로 스크롤 동기화 및 포커스(Highlight) 처리되어야 한다.

**Scenario 2-2: 퀵 메뉴(Quick Menu) 그리드 전환**
* **Given**: 화면 너비가 태블릿 해상도인 환경에서 메인 페이지를 스크롤하여 퀵 메뉴 영역에 도달했을 때
* **When**: 화면에 퀵 메뉴 아이콘 컨테이너가 렌더링되면
* **Then**: 아이콘들이 2x2 그리드 레이아웃으로 정확하게 배치되어야 하며, 각 버튼 내부의 메뉴 설명(Description)이 시각적으로 잘림 없이 표시되어야 한다.

---

## 3. 사이트맵 오버레이 및 모달 (Sitemap & Modal Interaction)

**Scenario 3-1: 사이트맵 오버레이 애니메이션 및 ARIA 상태 변경**
* **Given**: 사용자가 메인 페이지 최상단 헤더(Header)의 우측에 위치한 전체 메뉴(Sitemap) 토글 버튼을 확인한 상태에서
* **When**: 전체 메뉴 토글 버튼을 클릭(또는 터치/엔터)하면
* **Then**: 우측에서 좌측으로 부드러운 슬라이드-인(Slide-in) 애니메이션과 함께 사이트맵 메뉴 오버레이가 나타나야 하며, 해당 토글 버튼의 `aria-expanded` 속성값이 `false`에서 `true`로 즉시 변경되어야 한다.

**Scenario 3-2: 팝업 모달 닫기 (Collapse) 동작**
* **Given**: 메인 페이지에서 특정 도서 상세 정보나 공지사항 팝업 모달이 화면 중앙에 활성화되어 있는 상태에서
* **When**: 사용자가 모달 창 외부의 오버레이(Dimmed 영역)를 클릭하거나 모달 내부의 '닫기(Collapse)' 버튼을 누르면
* **Then**: 프리뷰 상태로 돌아가지 않고 모달 창이 화면에서 완전히 닫혀야 하며, 포커스는 모달을 열었던 기존 버튼 요소로 다시 돌아가야 한다.

---

## 4. 통합 검색 영역 (Search Container)

**Scenario 4-1: 검색 폼 입력 및 제출**
* **Given**: 사용자가 메인 페이지 중앙의 통합 검색 컨테이너 입력창(Input)에 포커스를 둔 상태에서
* **When**: "데이터베이스"라는 검색어를 입력하고 엔터 키를 누르거나 우측의 '검색(Submit)' 버튼을 클릭하면
* **Then**: 브라우저가 검색 결과 페이지 라우트로 이동해야 하며, 빈 값을 입력하고 제출할 경우 사용자에게 검색어 입력을 요구하는 적절한 피드백(안내 메시지 등)이 제공되어야 한다.
